-- Load core settings (options, keymaps) and the lazy.nvim plugin manager.
-- nvim-tree and every other plugin are managed by lazy.nvim (see lua/kkkubakkk/lazy.lua).
require("kkkubakkk.core")
require("kkkubakkk.lazy")
